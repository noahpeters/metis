{
  "name": "metis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "check": "find src scripts -type f -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "db:migrate:staging": "node scripts/require-github-actions.mjs && wrangler d1 migrations apply DB --remote",
    "deploy:staging": "node scripts/require-github-actions.mjs && wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
